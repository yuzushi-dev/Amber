[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    tests
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests (requires services)
    slow: Slow running tests

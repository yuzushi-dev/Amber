"""
Amber CLI
=========

Operator entry point for tasks not exposed in the admin UI:
- backup / restore / schedule
- tuning (prompts and parameters)
- llm provider / model selection
- evaluation runs (Locomo, etc. — staged)

Run ``amber --help`` to discover commands. The TUI is reachable through
``amber tui`` and wraps the same commands in an interactive interface.
"""
